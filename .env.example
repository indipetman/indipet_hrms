# Use mock while reviewing the migrated prototype. Change to api when the
# backend endpoints are connected in src/lib/hrms-api.js.
NEXT_PUBLIC_HRMS_DATA_MODE=mock
NEXT_PUBLIC_HRMS_API_BASE_URL=http://localhost:8000/api
